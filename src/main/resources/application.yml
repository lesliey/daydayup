server:
  display-name: daydayup
  port: 8081
spring:
  profiles:
    active: dev
  application:
    name: daydayup


management:
  security:
    enabled: false

logging:
  level:
    org.springframework.web: DEBUG
  pattern:
    console: '%d [%thread] [%X{trace_id:--}] %-5level %logger - %msg%n'
  path: /home/logs

endpoints:
  actuator:
    sensitive: false


